import RX = require('reactxp');
import 'jest';

import App = require('../App');

describe('App', () => {
    it('renders without crashing', () => {
        // Todo(3ygun): Figure out how to do this test
        // RX.App.initialize(true, true);
        // RX.UserInterface.setMainView(<App />);
    });
});
